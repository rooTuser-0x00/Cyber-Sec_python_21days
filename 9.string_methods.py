#formated staring 
course = "python for beginners"
print(len(course)) # to count length of all characters in course variable
print(course.upper()) # upper case
print(course.lower()) # lower case
print(course.find('f')) #it gives output in index format.
print(course.replace('f','t')) #replace f with t in string 
print(course.title()) # prints as same first line as the title.

print("python" in course) # to verify that the string is in course.
print(course[1:-1])